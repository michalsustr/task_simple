This is a simple example of a background task.
The task is downloading a file from the internet, displaying a progress bar and computes a CRC32 checksum.
Run with "trunk serve" for web, or "cargo run" for standard.
